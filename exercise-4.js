// Exercise 4
// Add the population density to each country in the array using the key name populationDensity.
// Population density is calculated by diving the population by the land area.
// Refer to country-data.js to see the data format.

function exerciseFour(countriesArray) {
    
}

module.exports = exerciseFour;